pywebview>=4.4
spotipy>=2.23
# yt-dlp >= 2026.3.17 required — older versions fail on YouTube (no JS runtime).
# The workflow installs the latest pre-release to ensure YouTube stays functional.
yt-dlp>=2026.3.17
mutagen
# SpotdlRip dependencies (see SpotdlRip/requirements.txt for full list)
# Install both: pip install -r requirements.txt -r SpotdlRip/requirements.txt
